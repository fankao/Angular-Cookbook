import { ReadTimeDirective } from './read-time.directive';

describe('RealTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadTimeDirective();
    expect(directive).toBeTruthy();
  });
});
